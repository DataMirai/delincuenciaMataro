---
title: ""
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

setwd("/Users/Mireia/Desktop/delincuenciaMataro")

library(tidyverse)
library(ggplot2)
library(plotly)
library(ggdark)
library(formattable)

source("Scripts/5_Poblacion_Delitos.R", encoding = "UTF-8")
source("Scripts/3_data_con_diccionarios.R", encoding = "UTF-8")

```

### INTRODUCCIÓN

Este informe expone los datos de delincuencia en Mataró desde el año 2016 hasta el 2020 proporcionados por la Generalitat de Catalunya. 

El objetivo principal es exponer de forma clara para los ciudadanos la realidad de la situación que se está viviendo en Mataró ante la ola de inseguridad que acecha a la ciudad y las numerosas quejas de los vecinos. 

 
### INDICACIONES PARA EL LECTOR

<div class="alert alert-info">
* Según el interés del lector, los casos se pueden observar por años y por barrios, seleccionando las pestañas ‘Por años’ y ’Por barrios’ respectivamente. 

* Los tipos de delito han sido agrupados en categorías para hacer más ameno su entendimiento, pero se pueden ver todos los delitos que recoge la base de datos en la pestaña del anexo.

* Los gráficos que componen el informe son interactivos, por lo que se puede seleccionar y deseleccionar los elementos de la leyenda, así como hacer zoom en partes concretas de los gráficos. Para deshacer el zoom, basta con hacer doble click en el gráfico.
</div>


**Escoge cómo quieres ver los datos:**

# {.tabset .tabset-fade}
## POR AÑOS {.tabset .tabset-fade}

En el número de delitos cometidos por años se observa un claro incremento desde 2017, destacándose 2020 como el año con más delitos registrados con diferencia, con 1.282 casos más que el año anterior. 

Aún así, si examinamos más de cerca los delitos registrados, cabe destacar que hay un tipo de delito que es el causante de esta gran diferencia entre 2020 y el año anterior: "Circular per la via publica excepte suposits rd 463/2020”. Esta ley es la que entró en vigor en marzo de 2020 para declarar el Estado de Alarma y la cuarentena, y en Mataró se pusieron un total de 1.058 denuncias a personas por saltarse la cuarentena injustificadamente. 

Eliminando esta diferencia de casos, el resultado es que en 2020 hubo 84 casos menos que en 2019, lo cual ha roto ligeramente la tendencia que estaba formándose desde 2017. 

####  {.tabset .tabset-pills}
##### Totales
```{r delitosany, echo=FALSE, warning=FALSE, message=FALSE}

policia_delitos_agreados_any<- policia%>%
  group_by(Any)%>%
  tally()%>%
  ggplot(aes(x= Any, y = n, fill= Any))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", Any, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Año", y= "Nº delitos", title= "Delitos por año", fill = "Año")+
  dark_theme_bw()

ggplotly(policia_delitos_agreados_any, source = "select", tooltip ='text')
```

##### Sin multas confinamiento
```{r delitosanyfiltered, echo=FALSE, warning=FALSE, message=FALSE}

policia_delitos_agreados_any_filtro<- policia%>%
  filter(tipus != "Circular via publica excepte suposits rd463/2020" & sub_tipus != "Otros")%>%
  group_by(Any)%>%
  tally()%>%
  ggplot(aes(x= Any, y = n, fill= Any))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", Any, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Año", y= "Nº delitos", title= "Delitos por año (sin multas por confinamiento)", subtitle= "Sin multas por confinamiento", fill = "Año")+
  dark_theme_bw()

ggplotly(policia_delitos_agreados_any_filtro, source = "select", tooltip ='text')
```

####  {-}

A continuación exponemos más concretamente las causas y las características de los delitos de cada año:


**Selecciona año:**

### 2016

En 2016 se cometieron un total de 2.383 delitos, entre los que se destacan los robos (469 casos), delitos que involucran drogas (342), delitos contra la integridad física (294) y contra la libertad a la intimidad (243).

<div class="alert alert-warning">
<strong> Apunte: </strong> Los delitos que tratamos están clasificados en 3 categorías según los afectados por éste: ‘Integridad’ en caso de que el delito se haya cometido contra la integridad de una persona, ‘Ofensas a la comunidad’ si el delito perjudica directa o indirectamente a un grupo de personas o a la comunidad entera, por ejemplo el consumo de drogas en un lugar público; y por último ‘Patrimonio’ para referirse a los delitos cometidos contra las propiedades de las personas o del Estado.
</div>

Si nos centramos en las categorías de los afectados por los delitos, se observa que en 2016 se cometieron un total de 717 delitos contra la integridad, 929 delitos de ofensas a la comunidad y 737 contra el patrimonio.

####  {.tabset .tabset-pills}
##### Delitos por tipo
```{r delitossubtipus, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_subtipus<-policia%>%
  filter(Any == 2016) %>%
  group_by(sub_tipus, Any)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=sub_tipus))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por subtipo", fill = "Subtipo")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_subtipus, source = "select", tooltip ='text')   
```


##### Delitos por categoría

```{r delitocategoria, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_categoria<-policia%>%
  filter(Any == 2016) %>%
  group_by(sub_tipus, Any, tipus_general)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=tipus_general))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>%s: %s", tipus_general, sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por categoría", fill = "Categorías")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_categoria, source = "select", tooltip ='text')     
```

####  {-}

Por lo tanto, podemos concluir que pese a que los robos son el delito más cometido, en su conjunto aquellos que afectan a toda la comunidad como los delitos por consumo de drogas, portación de armas, o vandalismo son los más numerosos. 

 **Barrios en 2016**

En cuanto a los barrios, se observa una tendencia extremista: los barrios con más población tienen muchos casos de delincuencia, mientras que los barrios con menor población tienen considerablemente menos casos. Esto se debe en parte a la manera en la que está distribuida la población del municipio, puesto que hay mucha diferencia de población entre los barrios más poblados de Mataró (Eixample, Cerdanyola, Rocafonda…) y los menos poblados (La Llàntia, el Centre…). 

En 2016 vemos que Pla d’en Boet se posiciona como el barrio con más delitos (402) mientras que La Llàntia es con diferencia el barrio con menor tasa de delincuencia (20). Aún así, de este gráfico hay que destacar dos puntos: el barrio de Cerdanyola está dividido en parte norte y parte sur, pero al sumar los delitos de ambas partes, el resultado es que Cerdanyola es el barrio con más delitos de toda la ciudad. El segundo punto a destacar es que los datos obtenidos por la Generalitat contienen nombres de barrios sin especificar como “Barri de Mataró”, “Resta del Terme” y “Sense especificar”. Esta sección recoge un total de 394 delitos no clasificados.

<div class="alert alert-warning">
Al observar el gráfico llama la atención que el barrio de Rocafonda, siendo uno de los más poblados del municipio, tenga en proporción una tasa tan baja de delitos, por lo que podría considerarse la posibilidad de que algunos de los delitos que no especifican el barrio en el que se han cometido pertenezcan a Rocafonda. 
</div>

####  {.tabset .tabset-pills}
##### Delitos por barrio

```{r delitosbarrio2016, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitosbarrio2016<- policia%>%
  filter(Any == 2016) %>%
  group_by(barri)%>%
  tally()%>%
  ggplot(aes(x=barri, y= n, fill=barri))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", barri, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Barrio", y= "Nº delitos", title= "Delitos por barrio", fill = "Barrios")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitosbarrio2016, source = "select", tooltip ='text')
```

####  {-}

Si nos centramos ahora en el tipo de delito que más se ha cometido en cada barrio a lo largo de 2016, se observa que cada barrio tiene una “especialidad”, es decir, hay un tipo de delito que sobresale del resto.

Predominan los robos o los delitos que involucran drogas en la mayoría de barrios, pero también se destacan las agresiones físicas en Cerdanyola y la resistencia o desobediencia a la autoridad en Pla d’en Boet.

```{r delitosubtipussbarrio2016, echo=FALSE, fig.height= 12, out.width= "100%", warning=FALSE, message=FALSE}

delitosubtipussbarrio2016<- policia%>%
  filter(Any == 2016) %>%
  group_by(barri, sub_tipus)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=sub_tipus))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>%s: %s", barri, sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  facet_wrap(~barri, ncol=3)+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por subtipo y barrio", fill = "Subtipo")+
  dark_theme_bw()+
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust=1))


ggplotly(delitosubtipussbarrio2016, source = "select", tooltip ='text')
```

Esta información se puede ver con más detalle en la pestaña “Por Barrios”.

 **Perfil del delincuente**

Si nos fijamos ahora en el perfil de los delincuentes, encontramos que la gran mayoría son hombres (1.911 casos que suponen el 80,33% del total), mientras que las mujeres son las autoras de 468 casos (el 19,67%).

La distribución por edades determina que la mayoría de los delitos son cometidos por personas jóvenes, de entre 17 y 26 años, aunque en el gráfico se destacan notablemente también las personas de 34 años.

En conclusión, podemos determinar que el perfil más común de los delincuentes de Mataró son hombres jóvenes de entre 17 y 30 años.

####  {.tabset .tabset-pills}
##### Delitos por sexo

```{r delitossexo2016, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitossexo2016<- policia%>%
  filter(Any == 2016) %>%
  group_by( sexe, Any)%>%
  tally()%>%
  ggplot(aes(x=sexe, y= n, fill=sexe))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", sexe, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Sexo", y= "Nº delitos", title= "Delitos por sexo", fill = "Sexo")+
  dark_theme_bw()

ggplotly(delitossexo2016, source = "select", tooltip ='text')
```

##### Delitos por edad

```{r delitosedad2016, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitosedad2016<- policia%>%
  filter(Any == 2016 & edat != 999) %>%
  group_by(edat)%>%
  tally()%>%
  ggplot(aes(x=edat, y=n))+
   geom_bar(position="stack", stat = "identity", aes(text=sprintf("Edad: %s<br>Delitos: %s", edat, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Edad", y= "Nº delitos", title= "Delitos por edad")+
  dark_theme_bw()

ggplotly(delitosedad2016, source = "select", tooltip ='text')
```

####  {-}


<div class="faa">
[<i float= right class="btn btn-light fa fa-arrow-up fa-4x" ></i>](#)
</div>

### 2017

En 2017 se cometieron un total de 2.240 delitos, 133 menos que el año anterior. Este año también estan a la cabeza los delitos por robo (403 casos), pero los delitos contra la integridad física (303) superan a los que involucran drogas (287). 

<div class="alert alert-warning">
<strong> Apunte: </strong> Los delitos que tratamos están clasificados en 3 categorías según los afectados por éste: ‘Integridad’ en caso de que el delito se haya cometido contra la integridad de una persona, ‘Ofensas a la comunidad’ si el delito perjudica directa o indirectamente a un grupo de personas o a la comunidad entera, por ejemplo el consumo de drogas en un lugar público; y por último ‘Patrimonio’ para referirse a los delitos cometidos contra las propiedades de las personas o del Estado.
</div>

Si nos centramos en las categorías de los afectados por los delitos, los números no son demasiado distintos a los del año anterior, pero se observa una ligera bajada en los delitos de ofensas a la comunidad (805) y contra el patrimonio (686). Aún así, hay un aumento en cuanto a los delitos contra la integridad (750). 

####  {.tabset .tabset-pills}
##### Delitos por tipo
```{r delitossubtipus2017, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_subtipus2017<-policia%>%
  filter(Any == 2017) %>%
  group_by(sub_tipus, Any)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=sub_tipus))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por subtipo", fill = "Subtipo")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_subtipus2017, source = "select", tooltip ='text') 
```

##### Delitos por categoría

```{r delitocategoria2017, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_categoria2017<-policia%>%
  filter(Any == 2017) %>%
  group_by(sub_tipus, Any, tipus_general)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=tipus_general))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>%s: %s", tipus_general, sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por categoría", fill = "Categorías")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_categoria2017, source = "select", tooltip ='text')  
```

####  {-}

Por lo tanto, podemos concluir que pese a que los robos son el delito más cometido, en su conjunto aquellos que afectan a toda la comunidad como los delitos por consumo de drogas, portación de armas, o vandalismo son los más numerosos. 

**Barrios en 2017**

En cuanto a los barrios, sigue existiendo la tendencia de que los barrios con más población tienen muchos más casos, mientras que los barrios con menos delitos son los que tienen considerablemente menos población. Esto se debe en parte a la manera en la que está distribuida la población del municipio, puesto que hay mucha diferencia de población entre los barrios más poblados de Mataró (Eixample, Cerdanyola, Rocafonda…) y los menos poblados (La Llàntia, el Centre…). 

En 2016 vemos que Pla d’en Boet se posiciona como el barrio con más delitos, 380, aunque son menos que el año anterior. La Llàntia sigue siendo el barrio más seguro con sólo 11 delitos registrados. Al juntar los datos registrados de las dos partes de Cerdanyola (Norte y Sur), éste se posiciona como el barrio con más delitos (443), siendo el único que ha aumentado respecto al año anterior. 

Otro punto a tener en cuenta es que los datos obtenidos por la Generalitat contienen nombres de barrios sin especificar como “Barri de Mataró”, “Resta del Terme” y “Sense especificar”. Esta sección recoge un total de 195 delitos no clasificados, 200 menos que en 2016.

<div class="alert alert-warning">
Al observar el gráfico llama la atención que el barrio de Rocafonda, siendo uno de los más poblados del municipio, tenga en proporción una tasa tan baja de delitos, por lo que podría considerarse la posibilidad de que algunos de los delitos que no especifican el barrio en el que se han cometido pertenezcan a Rocafonda. 
</div>

####  {.tabset .tabset-pills}
##### Delitos por barrio

```{r delitosbarrio2017, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitosbarrio2017<- policia%>%
  filter(Any == 2017) %>%
  group_by(barri)%>%
  tally()%>%
  ggplot(aes(x=barri, y= n, fill=barri))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", barri, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Barrio", y= "Nº delitos", title= "Delitos por barrio", fill = "Barrios")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitosbarrio2017, source = "select", tooltip ='text')
```

####  {-}

Si nos centramos ahora en el tipo de delito que más se ha cometido en cada barrio a lo largo de 2017, se observa una tendencia muy similar a las de 2016: cada barrio tiene una “especialidad”, es decir, hay un tipo de delito que sobresale del resto.

Predominan los robos o los delitos que involucran drogas en la mayoría de barrios, pero también se destacan las agresiones físicas y contra la libertad a la intimidad en Cerdanyola y la resistencia o desobediencia a la autoridad en Pla d’en Boet. Llama la atención también el aumento de robos en el barrio Vista Alegre, pasando de 34 en 2016 a 94 en 2017.

```{r delitosubtipussbarrio2017, echo=FALSE, fig.height= 12, out.width= "100%", warning=FALSE, message=FALSE}

delitosubtipussbarrio2017<- policia%>%
  filter(Any == 2017) %>%
  group_by(barri, sub_tipus)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=sub_tipus))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>%s: %s", barri, sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  facet_wrap(~barri, ncol=3)+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por subtipo y barrio", fill = "Subtipo")+
  dark_theme_bw()+
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust=1))


ggplotly(delitosubtipussbarrio2017, source = "select", tooltip ='text')
```

Esta información se puede ver con más detalle en la pestaña “Por Barrios”.

**Perfil del delincuente**

Si nos fijamos ahora en el perfil de los delincuentes, encontramos que la gran mayoría siguen siendo hombres (1.780 casos), mientras que las mujeres son las autoras de 461 casos. Esto supone una proporción de 79,4% de hombres y un 20,6% de mujeres, una diferencia ligeramente más corta que en el año anterior.

La distribución por edades es bastante distinta respecto a 2016. Si bien es cierto que gran parte de los delitos se concentran en la franja de los 18 a los 25 años, entre los 30 y los 39, se observan picos considerables que agrupan numerosos delitos. Entonces, mientras que en 2016 los delitos estaban más concentrados entre la población joven, en 2017 están más repartidos entre una franja de edades más amplia.

####  {.tabset .tabset-pills}
##### Delitos por sexo

```{r delitossexo2017, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitossexo2017<- policia%>%
  filter(Any == 2017) %>%
  group_by( sexe)%>%
  tally()%>%
  ggplot(aes(x=sexe, y= n, fill=sexe))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", sexe, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Sexo", y= "Nº delitos", title= "Delitos por sexo", fill = "Sexo")+
  dark_theme_bw()

ggplotly(delitossexo2017, source = "select", tooltip ='text')
```

##### Delitos por edad

```{r delitosedad2017, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitosedad2017<- policia%>%
  filter(Any == 2017 & edat != 999) %>%
  group_by(edat)%>%
  tally()%>%
  ggplot(aes(x=edat, y=n))+
   geom_bar(position="stack", stat = "identity", aes(text=sprintf("Edad: %s<br>Delitos: %s", edat, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Edad", y= "Nº delitos", title= "Delitos por edad")+
  dark_theme_bw()

ggplotly(delitosedad2017, source = "select", tooltip ='text')
```

####  {-}

<div class="faa">
[<i float= right class="btn btn-light fa fa-arrow-up fa-4x" ></i>](#)
</div>

### 2018

En 2018 se notificaron 2.304 delitos, apenas 60 más que el año anterior. Este año también estan a la cabeza los delitos por robo (406 casos) que se mantienen en número respecto al año anterior. Los siguen los delitos por drogas (304), que experimentan un pequeño repunte y vuelven a colocarse en segunda posición, y en tercer lugar se encuentran los delitos contra la integridad física (296) con una ligera disminución respecto a 2017.

<div class="alert alert-warning">
<strong> Apunte: </strong> Los delitos que tratamos están clasificados en 3 categorías según los afectados por éste: ‘Integridad’ en caso de que el delito se haya cometido contra la integridad de una persona, ‘Ofensas a la comunidad’ si el delito perjudica directa o indirectamente a un grupo de personas o a la comunidad entera, por ejemplo el consumo de drogas en un lugar público; y por último ‘Patrimonio’ para referirse a los delitos cometidos contra las propiedades de las personas o del Estado.
</div>

En cuanto a las categorías de los afectados por los delitos, encontramos que la tendencia cambió bastante. En 2018 predominaron los delitos contra el patrimonio (809) con más de 100 casos más que en 2017. Los delitos de ofensas a la comunidad (788) quedan relevados al segundo puesto con una ligera bajada, seguida por los delitos contra la integridad (711).

####  {.tabset .tabset-pills}
##### Delitos por tipo
```{r delitossubtipus2018, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_subtipus2018<-policia%>%
  filter(Any == 2018) %>%
  group_by(sub_tipus, Any)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=sub_tipus))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por subtipo", fill = "Subtipo")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_subtipus2018, source = "select", tooltip ='text') 
```

##### Delitos por categoría

```{r delitocategoria2018, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_categoria2018<-policia%>%
  filter(Any == 2018) %>%
  group_by(sub_tipus, Any, tipus_general)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=tipus_general))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>%s: %s", tipus_general, sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por categoría", fill = "Categorías")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_categoria2018, source = "select", tooltip ='text')   
```

####  {-}

Por lo tanto, podemos concluir que 2018 se caracteriza por ser un año con una tasa elevada de bandalismo que afectó a la propiedad privada y pública de la ciudad. También hay que destacar el incremento de denuncias por fraude, que aumentó en más de 100 respecto a 2017.

**Barrios en 2018**

En cuanto a los barrios, sigue existiendo la tendencia de que los barrios con más población tienen muchos delitos, mientras que los barrios con menos población tienen considerablemente pocos. Esto se debe en parte a la manera en la que está distribuida la población del municipio, puesto que hay mucha diferencia de población entre los barrios más poblados de Mataró (Eixample, Cerdanyola, Rocafonda…) y los menos poblados (La Llàntia, el Centre…). 

Aún así, en 2018 se ha reducido notablemente las diferencias que existían entre los delitos por barrios, y en prácticamente todos ha aumentado el número de delitos respecto al año anterior.

En 2018 vemos que el Eixample se posiciona como el barrio con más delitos (354), quitándole la primera posición a Pla d’en Boet (327), que ha experimentado una considerable reducción respecto a 2017.La Llàntia sigue siendo el barrio con menos delitos, pero ha doblado la cantidad, pasando de 11 en 2017 a 28 este año. Al juntar los datos registrados de las dos partes de Cerdanyola (Norte y Sur), encontramos que sigue siendo el barrio con más delitos de la ciudad (445).

Otro punto a tener en cuenta es que los datos obtenidos por la Generalitat contienen nombres de barrios sin especificar como “Barri de Mataró”, “Resta del Terme” y “Sense especificar”. Esta sección recoge un total de 203 delitos no clasificados, una cifra muy parecida a la de 2017.

<div class="alert alert-warning">
Al observar el gráfico llama la atención que el barrio de Rocafonda, siendo uno de los más poblados del municipio, tenga en proporción una tasa tan baja de delitos, por lo que podría considerarse la posibilidad de que algunos de los delitos que no especifican el barrio en el que se han cometido pertenezcan a Rocafonda. 
</div>

####  {.tabset .tabset-pills}
##### Delitos por barrio

```{r delitosbarrio2018, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitosbarrio2018<- policia%>%
  filter(Any == 2018) %>%
  group_by(barri)%>%
  tally()%>%
  ggplot(aes(x=barri, y= n, fill=barri))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", barri, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Barrio", y= "Nº delitos", title= "Delitos por barrio", fill = "Barrios")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitosbarrio2018, source = "select", tooltip ='text')
```

####  {-}

Si nos centramos ahora en el tipo de delito que más se ha cometido en cada barrio a lo largo de 2018, se observa una tendencia muy similar a las de los años anteriores: cada barrio tiene una “especialidad”, es decir, hay un tipo de delito que sobresale del resto.

Predominan los robos o los delitos que involucran drogas en la mayoría de barrios, pero también se destacan las agresiones físicas y contra la libertad a la intimidad en Cerdanyola y la resistencia o desobediencia a la autoridad en Pla d’en Boet. Llama la atención también el número de robos en el barrio Vista Alegre, que se mantiene en 93 como el año anterior, pero es una cifra que se ha triplicado desde 2016.

```{r delitosubtipussbarrio2018, echo=FALSE, fig.height= 12, out.width= "100%", warning=FALSE, message=FALSE}

delitosubtipussbarrio2018<- policia%>%
  filter(Any == 2018) %>%
  group_by(barri, sub_tipus)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=sub_tipus))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>%s: %s", barri, sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  facet_wrap(~barri, ncol=3)+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por subtipo y barrio", fill = "Subtipo")+
  dark_theme_bw()+
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust=1))


ggplotly(delitosubtipussbarrio2018, source = "select", tooltip ='text')
```

Esta información se puede ver con más detalle en la pestaña “Por Barrios”.

**Perfil del delincuente**

Si nos fijamos ahora en el perfil de los delincuentes, encontramos que la gran mayoría siguen siendo hombres (1.822 casos), mientras que las mujeres son las autoras de 487 casos. Esto supone una proporción de 78,9% de hombres y un 21,1% de mujeres, una diferencia que va disminuyendo ligeramente respecto a los años anteriores.

La distribución por edades es bastante distinta a la de los años anteriores. Si bien la mayoría de delitos se concentran en la población más jóven, sobresalen picos notables en los 23 y los 29 años. Dicho esto, podemos concluir que la mayoría de delitos se concentran en la franja de entre los 19 y los 36, sin dejar de lado el pico de los 39. Por lo tanto, contamos con una franja más amplia que en años anteriores.

####  {.tabset .tabset-pills}
##### Delitos por sexo

```{r delitossexo2018, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitossexo2018<- policia%>%
  filter(Any == 2018) %>%
  group_by( sexe)%>%
  tally()%>%
  ggplot(aes(x=sexe, y= n, fill=sexe))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", sexe, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Sexo", y= "Nº delitos", title= "Delitos por sexo", fill = "Sexo")+
  dark_theme_bw()

ggplotly(delitossexo2018, source = "select", tooltip ='text')
```

##### Delitos por edad

```{r delitosedad2018, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitosedad2018<- policia%>%
  filter(Any == 2018 & edat != 999) %>%
 group_by(edat)%>%
  tally()%>%
  ggplot(aes(x=edat, y=n))+
   geom_bar(position="stack", stat = "identity", aes(text=sprintf("Edad: %s<br>Delitos: %s", edat, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Edad", y= "Nº delitos", title= "Delitos por edad")+
  dark_theme_bw()

ggplotly(delitosedad2018, source = "select", tooltip ='text')
```

####  {-}

<div class="faa">
[<i float= right class="btn btn-light fa fa-arrow-up fa-4x" ></i>](#)
</div>

### 2019

En 2019 se notificaron 2.475 delitos, un incremento notable de 170 más que en 2018. Este año también estan a la cabeza los robos, que han aumentado hasta los 472 (hubo 406 en 2018). Los siguen los delitos contra la integridad física (308), que han aumentado ligeramente y han superado a los delitos por drogas (292).

También llama la atención el aumento de delitos de resistencia o desobediencia a la autoridad (un 23,83% más) y contra la propiedad privada (un 65,03% más) respecto a 2018. Aún así, los delitos por fraude se han reducido considerablemente, pasando de 216 en 2018 a 151.

<div class="alert alert-warning">
<strong> Apunte: </strong> Los delitos que tratamos están clasificados en 3 categorías según los afectados por éste: ‘Integridad’ en caso de que el delito se haya cometido contra la integridad de una persona, ‘Ofensas a la comunidad’ si el delito perjudica directa o indirectamente a un grupo de personas o a la comunidad entera, por ejemplo el consumo de drogas en un lugar público; y por último ‘Patrimonio’ para referirse a los delitos cometidos contra las propiedades de las personas o del Estado.
</div>

En cuanto a las categorías de los afectados por los delitos, encontramos que la tendencia cambiante que se produjo el año pasado se acrecenta. Los delitos contra el patrimonio se sitúan en primera posición con 921 casos, 112 más que el año pasado y 220 más que en 2017. Los delitos de ofensas a la comunidad también aumentan bastante, pasando de los 788 casos de 2018 a 845. En cuanto a los delitos contra la integridad (710), no hay ningún cambio respecto al año anterior.

####  {.tabset .tabset-pills}
##### Delitos por tipo
```{r delitossubtipus2019, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_subtipus2019<-policia%>%
  filter(Any == 2019) %>%
  group_by(sub_tipus, Any)%>%
  tally()%>%
 ggplot(aes(x=sub_tipus, y= n, fill=sub_tipus))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por subtipo", fill = "Subtipo")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_subtipus2019, source = "select", tooltip ='text') 
```

##### Delitos por categoría

```{r delitocategoria2019, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_categoria2019<-policia%>%
  filter(Any == 2019 & tipus_general != "Otros") %>%
  group_by(sub_tipus, Any, tipus_general)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=tipus_general))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>%s: %s", tipus_general, sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por categoría", fill = "Categorías")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_categoria2019, source = "select", tooltip ='text')   
```

####  {-}


En conclusión, en 2019 continúa la tendencia al alza de delitos, con robos, agresiones físicas y delitos por drogas a la cabeza. Además, cabe destacar que continúa la elevada tasa de bandalismo contra el patrimonio privado y público de la ciudad con un aumento de 112 delitos más que en el año anterior. 

**Barrios en 2019**

En cuanto a los barrios, sigue existiendo la tendencia de que los barrios con mayor población tienen más casos que los de menor. Esto se debe en parte a la manera en la que está distribuida la población del municipio, puesto que hay mucha diferencia de población entre los barrios más poblados de Mataró (Eixample, Cerdanyola, Rocafonda…) y los menos poblados (La Llàntia, el Centre…). 

La tendencia entre barrios es muy similar a la del año anterior, la mayoría de barrios han experimentado un aumento en la delincuencia, pero de forma muy leve. Los barrios que sí han experimentado un crecimiento considerable son L'Eixample, que ha pasado de tener 354 casos en 2018 a 412 en 2019; y Cerdanyola, que en su conjunto suma 524 delitos, 79 más que el año anterior.

Esto hace que Cerdanyola siga posicionándose como el barrio con más delincuencia (524), seguido por l'Eixample (412) y Pla d'en Boet (357). Los barrios con menor tasa de delincuencia son La Llàntia (22), y el Centre (63), que coinciden con los barrios menos poblados del municipio.

Otro punto a tener en cuenta es que los datos obtenidos por la Generalitat contienen nombres de barrios sin especificar como “Barri de Mataró” y “Sense especificar”. Esta sección recoge un total de 184 delitos no clasificados, una cifra muy parecida a la de los años anteriores, aunque este año no cuenta con la clasificación "Resta del terme".

<div class="alert alert-warning">
Al observar el gráfico llama la atención que el barrio de Rocafonda, siendo uno de los más poblados del municipio, tenga en proporción una tasa tan baja de delitos, por lo que podría considerarse la posibilidad de que algunos de los delitos que no especifican el barrio en el que se han cometido pertenezcan a Rocafonda. 
</div>

####  {.tabset .tabset-pills}
##### Delitos por barrio

```{r delitosbarrio2019, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitosbarrio2019<- policia%>%
  filter(Any == 2019) %>%
  group_by(barri)%>%
  tally()%>%
  ggplot(aes(x=barri, y= n, fill=barri))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", barri, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Barrio", y= "Nº delitos", title= "Delitos por barrio", fill = "Barrios")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitosbarrio2019, source = "select", tooltip ='text')
```

####  {-}

Si nos centramos ahora en el tipo de delito que más se ha cometido en cada barrio a lo largo de 2019, se observa que se diluye la tendencia de "especialidades" en cada barrio. Esta vez no hay un delito que sobresalga notoriamente del resto, a excepción del Eixample y sobretodo Vista Alegre, donde los robos se disparan en comparación con otros delitos y pasando de 93 en 2018 a 118 en 2019. Una cifra alarmante si se compara con los 37 robos que hubo en el barrio en 2016. 

También llama la atención el aumento de delitos contra la propiedad privada en Cerdanyola Norte, que ha pasado de 20 casos en 2018 a 43 este año. Así como en Cerdanyola Sur, que ha aumentado desde los 24 casos en 2018 hasta los 46.

Además, se ha doblado la cifra de las denuncias por resistencia a la autoridad en el barrio del Eixample pasande de 26 casos en 2018 a 52, así como los robos, que han aumentado de 56 casos hasta los 91. 

Los robos siguen en primera posición como delito más cometido en la mayoría de barrios, pero también se destacan los delitos contra la integridad física, los cuales han adelantado a los que involucran drogas respecto al año anterior.


```{r delitosubtipussbarrio2019, echo=FALSE, fig.height= 12, out.width= "100%", warning=FALSE, message=FALSE}

delitosubtipussbarrio2019<- policia%>%
  filter(Any == 2019) %>%
  group_by(barri, sub_tipus)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=sub_tipus))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>%s: %s", barri, sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  facet_wrap(~barri, ncol=3)+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por subtipo y barrio", fill = "Subtipo")+
  dark_theme_bw()+
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust=1))


ggplotly(delitosubtipussbarrio2019, source = "select", tooltip ='text')
```

Esta información se puede ver con más detalle en la pestaña “Por Barrios”.

**Perfil del delincuente**

Si nos fijamos ahora en el perfil de los delincuentes, encontramos que la gran mayoría siguen siendo hombres (1.926 casos), mientras que las mujeres son las autoras de 551 casos. Esto supone una proporción de 77,75% de hombres y un 22,25% de mujeres, una diferencia que se va acortando ligeramente a lo largo de los años.

La distribución por edades es cada vez más amplia. En los años anteriores se diferenciaba claramente que eran los más jóvenes los que más delitos cometía. Si bien en 2018 la franja que más delitos cometía se encontraba entre los 19 y los 36 años, en 2019 esta franja se expande desde los 17 años hasta los 40.

####  {.tabset .tabset-pills}
##### Delitos por sexo

```{r delitossexo2019, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitossexo2019<- policia%>%
  filter(Any == 2019) %>%
  group_by( sexe)%>%
  tally()%>%
  ggplot(aes(x=sexe, y= n, fill=sexe))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", sexe, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Sexo", y= "Nº delitos", title= "Delitos por sexo", fill = "Sexo")+
  dark_theme_bw()

ggplotly(delitossexo2019, source = "select", tooltip ='text')
```

##### Delitos por edad

```{r delitosedad2019, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitosedad2019<- policia%>%
  filter(Any == 2019 & edat != 999) %>%
  group_by(edat)%>%
  tally()%>%
  ggplot(aes(x=edat, y=n))+
   geom_bar(position="stack", stat = "identity", aes(text=sprintf("Edad: %s<br>Delitos: %s", edat, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Edad", y= "Nº delitos", title= "Delitos por edad")+
  dark_theme_bw()

ggplotly(delitosedad2019, source = "select", tooltip ='text')
```

####  {-}

<div class="faa">
[<i float= right class="btn btn-light fa fa-arrow-up fa-4x" ></i>](#)
</div>

### 2020

2020 ha sido un año marcado por la pandemia y el confinamiento, y este hecho también se ve reflejado en el número de delitos cometidos. El hecho más destacable ha sido el aumento de denúncias interpuestas a lo largo del año, lo cual dispara las cifras de 2.477 en 2019 a 3.759. Esto se debe a que entre las cifras de 2020 también se encuentran las multas a las personas que se saltaron el confinamiento o los toques de queda durante el año. 

"Circular per la via publica excepte suposits rd 463/2020” es la ley que entró en vigor en marzo de 2020 para declarar el Estado de Alarma y la cuarentena, y en Mataró se pusieron un total de 1.058 denuncias a personas por saltarse la cuarentena injustificadamente. Eliminando dichas multas, la cifra de delitos quedaría en 2.393 casos, 84 casos menos que en 2019, aunque 89 casos por encima de la cifra de 2018.

Dejando de lado las multas del confinamiento, los robos siguen a la cabeza como el delito más cometido (397), aunque se ha experimentado un descenso notable respecto a 2019 (hubo 472) e incluso menos que en 2018 (406). Los siguen los delitos relacionados con resistencia a la autoridad (317), los cuales han experimentado un aumento notable (hubo 239 casos en 2019) que podría estar relacionado con las medidas impuestas por el confinamiento. 

Otros delitos que destacan por su aumento respecto a años anteriores son la categoría 'Otros', compuesta principalmente por la ley 4/2015 para la Seguridad Ciudadana (también conocida como Ley Mordaza) que cuenta con 307 casos que no aparecen en ningún otro año, los delitos relacionados con armas, que han pasado de 89 casos en 2019 a 124 en 2020; y los delitos por fraude, que han aumentado desde los 101 casos el año anterior hasta los 151. 


<div class="alert alert-warning">
<strong> Apunte: </strong> Los delitos que tratamos están clasificados en 3 categorías según los afectados por éste: ‘Integridad’ en caso de que el delito se haya cometido contra la integridad de una persona, ‘Ofensas a la comunidad’ si el delito perjudica directa o indirectamente a un grupo de personas o a la comunidad entera, por ejemplo el consumo de drogas en un lugar público; y por último ‘Patrimonio’ para referirse a los delitos cometidos contra las propiedades de las personas o del Estado.
</div>

En cuanto a las categorías de los afectados por los delitos, encontramos que tantos los delitos contra la integridad (634) como contra el patrimonio (795) se han visto reducidos considerablemente, con cifras cercanas a los niveles de 2018. Aún así, los delitos de ofensas a la comunidad han experimentado una subida drástica, teniendo en cuenta las multas se cuentan 2.329 casos en total, y sin tenerlas en cuenta la cifra se reduce a los 1.271 casos, 426 casos más de los 845 casos registrados en 2019.

####  {.tabset .tabset-pills}
##### Delitos por tipo
```{r delitossubtipus2020, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_subtipus2020<-policia%>%
  filter(Any == 2020) %>%
  group_by(sub_tipus, Any)%>%
  tally()%>%
 ggplot(aes(x=sub_tipus, y= n, fill=sub_tipus))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por subtipo", fill = "Subtipo")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_subtipus2020, source = "select", tooltip ='text') 
```

##### Delitos por tipo (sin multas)
```{r delitossubtipusmultas2020, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_subtipusmultas2020<-policia%>%
  filter(tipus != "Circular via publica excepte suposits rd463/2020" & sub_tipus != "Otros")%>%
  filter(Any == 2020) %>%
  group_by(sub_tipus, Any)%>%
  tally()%>%
 ggplot(aes(x=sub_tipus, y= n, fill=sub_tipus))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por subtipo (sin multas)", fill = "Subtipo")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_subtipusmultas2020, source = "select", tooltip ='text') 

```

##### Delitos por categoría
```{r delitocategoria2020, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_categoria2020<-policia%>%
  filter(Any == 2020 & tipus_general != "Otros") %>%
  group_by(sub_tipus, Any, tipus_general)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=tipus_general))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>%s: %s", tipus_general, sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por categoría", fill = "Categorías")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_categoria2020, source = "select", tooltip ='text')   
```

##### Delitos por categoría (sin multas)
```{r delitocategoriamulta2020, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitos_categoriamulta2020<-policia%>%
  filter(tipus != "Circular via publica excepte suposits rd463/2020")%>%
  filter(Any == 2020 & tipus_general != "Otros") %>%
  group_by(sub_tipus, Any, tipus_general)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=tipus_general))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>%s: %s", tipus_general, sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por categoría (sin multas)", fill = "Categorías")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitos_categoriamulta2020, source = "select", tooltip ='text') 

```


####  {-}


En conclusión, en 2020 los delitos se han reducido ligeramente, aunque por el momento todo apunta a que el confinamiento ha sido la principal causa de esta ruptura en la tendencia al alza. Los delitos más cometidos siguen siendo los robos, seguidos de delitos relacionados con las drogas y agresiones físicas. Estas últimas han experimentado un repunte importante, aumentando de 231 casos en 2019 a 308. Aún así, delitos como la ley 4/2015 para la Seguridad Ciudadana (también conocida como Ley Mordaza), los relacionados con armas y los casos de fraude han aumentado considerablemente.


**Barrios en 2020**

En cuanto a los barrios, sigue existiendo la tendencia de que los barrios con mayor población tienen más casos que los de menor. Esto se debe en parte a la manera en la que está distribuida la población del municipio, puesto que hay mucha diferencia de población entre los barrios más poblados de Mataró (Eixample, Cerdanyola, Rocafonda…) y los menos poblados (La Llàntia, el Centre…). 

La tendencia entre barrios es muy similar a la del año anterior, la mayoría de barrios han experimentado un aumento en la delincuencia, pero de forma muy leve. Los barrios que sí han experimentado un crecimiento considerable son L'Eixample, que ha pasado de tener 354 casos en 2018 a 412 en 2019; y Cerdanyola, que en su conjunto suma 524 delitos, 79 más que el año anterior.

Esto hace que Cerdanyola siga posicionándose como el barrio con más delincuencia (524), seguido por l'Eixample (412) y Pla d'en Boet (357). Los barrios con menor tasa de delincuencia son La Llàntia (22), y el Centre (63), que coinciden con los barrios menos poblados del municipio.

Otro punto a tener en cuenta es que los datos obtenidos por la Generalitat contienen nombres de barrios sin especificar como “Barri de Mataró” y “Sense especificar”. Esta sección recoge un total de 184 delitos no clasificados, una cifra muy parecida a la de los años anteriores, aunque este año no cuenta con la clasificación "Resta del terme".


####  {.tabset .tabset-pills}
##### Delitos por barrio

```{r delitosbarrio2020, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitosbarrio2020<- policia%>%
  filter(Any == 2020) %>%
  group_by(barri)%>%
  tally()%>%
  ggplot(aes(x=barri, y= n, fill=barri))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", barri, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Barrio", y= "Nº delitos", title= "Delitos por barrio", fill = "Barrios")+
  dark_theme_bw()+
  coord_flip()

ggplotly(delitosbarrio2020, source = "select", tooltip ='text')
```

####  {-}

Si nos centramos ahora en el tipo de delito que más se ha cometido en cada barrio a lo largo de 2019, se observa que se diluye la tendencia de "especialidades" en cada barrio. Esta vez no hay un delito que sobresalga notoriamente del resto, a excepción del Eixample y sobretodo Vista Alegre, donde los robos se disparan en comparación con otros delitos y pasando de 93 en 2018 a 118 en 2019. Una cifra alarmante si se compara con los 37 robos que hubo en el barrio en 2016. 

También llama la atención el aumento de delitos contra la propiedad privada en Cerdanyola Norte, que ha pasado de 20 casos en 2018 a 43 este año. Así como en Cerdanyola Sur, que ha aumentado desde los 24 casos en 2018 hasta los 46.

Además, se ha doblado la cifra de las denuncias por resistencia a la autoridad en el barrio del Eixample pasande de 26 casos en 2018 a 52, así como los robos, que han aumentado de 56 casos hasta los 91. 

Los robos siguen en primera posición como delito más cometido en la mayoría de barrios, pero también se destacan los delitos contra la integridad física, los cuales han adelantado a los que involucran drogas respecto al año anterior.


```{r delitosubtipussbarrio2020, echo=FALSE, fig.height= 12, out.width= "100%", warning=FALSE, message=FALSE}

delitosubtipussbarrio2020 <- policia%>%
  filter(Any == 2020) %>%
  group_by(barri, sub_tipus)%>%
  tally()%>%
  ggplot(aes(x=sub_tipus, y= n, fill=sub_tipus))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>%s: %s", barri, sub_tipus, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  facet_wrap(~barri, ncol=3)+
  labs(x= "Subtipo", y= "Nº delitos", title= "Delitos por subtipo y barrio", fill = "Subtipo")+
  dark_theme_bw()+
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust=1))


ggplotly(delitosubtipussbarrio2020, source = "select", tooltip ='text')
```

Esta información se puede ver con más detalle en la pestaña “Por Barrios”.

**Perfil del delincuente**

Si nos fijamos ahora en el perfil de los delincuentes, encontramos que la gran mayoría siguen siendo hombres (1.926 casos), mientras que las mujeres son las autoras de 551 casos. Esto supone una proporción de 77,75% de hombres y un 22,25% de mujeres, una diferencia que se va acortando ligeramente a lo largo de los años.

La distribución por edades es cada vez más amplia. En los años anteriores se diferenciaba claramente que eran los más jóvenes los que más delitos cometía. Si bien en 2018 la franja que más delitos cometía se encontraba entre los 19 y los 36 años, en 2019 esta franja se expande desde los 17 años hasta los 40.

####  {.tabset .tabset-pills}
##### Delitos por sexo

```{r delitossexo2020, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitossexo2020 <- policia%>%
  filter(Any == 2020 & sexe != "SENSE ESPECIFICAR") %>%
  group_by( sexe)%>%
  tally()%>%
  ggplot(aes(x=sexe, y= n, fill=sexe))+
  geom_bar(position="stack", stat = "identity", aes(text=sprintf("%s<br>Delitos: %s<br>", sexe, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Sexo", y= "Nº delitos", title= "Delitos por sexo", fill = "Sexo")+
  dark_theme_bw()

ggplotly(delitossexo2020, source = "select", tooltip ='text')
```

##### Delitos por edad

```{r delitosedad2020, echo=FALSE, out.width= "100%", warning=FALSE, message=FALSE}

delitosedad2020<- policia%>%
  filter(Any == 2020 & edat != 999) %>%
  group_by(edat)%>%
  tally()%>%
  ggplot(aes(x=edat, y=n))+
   geom_bar(position="stack", stat = "identity", aes(text=sprintf("Edad: %s<br>Delitos: %s", edat, prettyNum(n, big.mark = ".", decimal.mark=","))))+
  labs(x= "Edad", y= "Nº delitos", title= "Delitos por edad")+
  dark_theme_bw()

ggplotly(delitosedad2020, source = "select", tooltip ='text')
```

####  {-}

<div class="faa">
[<i float= right class="btn btn-light fa fa-arrow-up fa-4x" ></i>](#)
</div>

## POR BARRIOS {.tabset .tabset-fade}

#  {-}

